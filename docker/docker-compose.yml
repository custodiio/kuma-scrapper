version: "3.9"

services:
  scraper:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    env_file:
      - ../.env
    environment:
      - HISTORY_DB_PATH=/data/history.db
      - DOUYIN_API_BASE=http://localhost:5555
    volumes:
      - ../data:/data
    network_mode: host
